const prompt = require("prompt-sync")(); // adiciona o pacote 'prompt-sync' ao código (entrada de dados)
const num1 = Number(prompt("Digite o primeiro número: ")); // entrada de dados
const num2 = Number(prompt("Digite o segundo número: ")); // entrada de dados
const soma = num1 + num2;
console.log("A soma dos números é: ", soma); // saída de dados
